# Generated by Cargo
# will have compiled files and executables
/target/
# iCloud Drive 가 동기화 못 하게 .nosync 접미사를 쓴 빌드 산출물 디렉토리
# (.cargo/config.toml 의 target-dir 가 여기를 가리킴 — iCloud mtime churn → 매번 풀 빌드 방지)
/target.nosync/
/gen/schemas
